{
  "name": "literacy-assessment-monorepo",
  "private": true,
  "scripts": {
    "vercel-build": "cd backend && npm install && npm run vercel-build && cd .. && cp -r backend/src . && cp -r backend/prisma . && cp backend/.env.production .env"
  }
}
